// A controlled tool contract, not a copy of private account model settings.
// Preserve the chosen provider/model and known public UI metadata. Per-model
// hidden prompt/context metadata is intentionally not inferred from model/list.
export function modelProfile(model,metadata={}) {
 return {models:[{slug:model,display_name:metadata.displayName??model,description:metadata.description??null,supported_reasoning_levels:(metadata.supportedReasoningEfforts??[]).map(r=>({effort:r.reasoningEffort,description:r.description})),default_reasoning_level:metadata.defaultReasoningEffort??null,shell_type:'disabled',visibility:'list',supported_in_api:true,priority:0,support_verbosity:false,default_verbosity:null,apply_patch_tool_type:null,truncation_policy:{mode:'bytes',limit:10000},experimental_supported_tools:[],tool_mode:'direct',multi_agent_version:'disabled',supports_search_tool:false,node_repl_disabled:true,input_modalities:metadata.inputModalities??['text'],include_apps_usage_instructions:false,include_skills_usage_instructions:false,include_plugin_usage_instructions:false,base_instructions:'You are Codex working on the selected RemoteDesk project. Use only the provided RemoteDesk tools. Project commands run in /workspace in an isolated container and require a one-time controller approval. The container has no network access or host paths outside the selected project. Read and follow applicable project instructions using the workspace read tool. Ask the controller non-secret questions when needed. Never request credentials or attempt to bypass the tool boundary. Explain changes and verification accurately.'}]};
}
