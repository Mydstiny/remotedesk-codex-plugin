---
name: remotedesk-setup
description: Check and explain the RemoteDesk Codex AI0 integration when a user asks to prepare or diagnose RemoteDesk remote AI. This version supports local diagnostics only; it cannot deploy a remote service.
---

# RemoteDesk setup

Read ../../docs/agent-deploy.md and ../../docs/compatibility.md before running commands. Resolve commands against this plugin root. Run `node bin/remotedesk-codex.mjs doctor --json` for the current version checks; run `probe --json` only within the user's requested local integration checks.

Never claim that doctor success enables phone pairing. This alpha has no install, start, pair, network listener, remote tools, or purchase-entitlement authority. Do not expose the official App Server directly. Do not modify user accounts, operational settings, profiles, firewall rules, or auto-start services. Preserve runtime warnings and report exact checked versions, exit codes, and the untested execution/approval/resume boundary.
