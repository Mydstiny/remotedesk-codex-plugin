---
name: remotedesk-setup
description: Install, configure, diagnose and operate the RemoteDesk Codex host bridge and its paired reference client.
---

Read ../../README.md, ../../SECURITY.md, ../../compatibility.json and ../../docs/agent-deploy.md first, then follow ../../docs/operations.md. The package includes a separate background service; activating this skill does not start it.

Use the user's existing local provider configuration without reading or printing credentials. Install a verified fixed release into a persistent version directory. Default to loopback and a private state directory outside all selected projects. Keep the exact engine version gate, TLS validation, dynamic-tool boundary and Docker project execution controls. Never expose raw App Server or invent a Pro purchase receipt.

Perform already authorized setup, pairing and service actions without repetitive permission prompts. Missing project/host/port choices should be resolved from the user's context before asking a focused question. Keep operation IDs and epochs on uncertain writes; reconcile before creating a new operation. Preserve user projects, unrelated local sessions, provider settings and state on uninstall. Report the actual host/model/device test boundary. Do not begin HarmonyOS implementation unless separately requested.
